% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lillardhaz.R
\name{fit_lillardhaz}
\alias{fit_lillardhaz}
\title{Fit a Lillard-style simultaneous-equations hazard/probit model}
\usage{
fit_lillardhaz(
  eq1,
  eq2,
  data,
  y1,
  d1 = NULL,
  y2,
  d2,
  x1 = character(0),
  x2 = character(0),
  nodes1 = NULL,
  nodes2 = NULL,
  corr = TRUE,
  start = NULL,
  method = "BFGS"
)
}
\arguments{
\item{eq1}{one of \code{"probit"}, \code{"lognormal"}, \code{"pgompertz"}.}

\item{eq2}{one of \code{"lognormal"}, \code{"pgompertz"}.}

\item{data}{a data frame.}

\item{y1}{name of equation 1's outcome: the binary 0/1 variable if
\code{eq1="probit"}, otherwise the duration variable.}

\item{d1}{name of equation 1's failure indicator (ignored if
\code{eq1="probit"}).}

\item{y2}{name of equation 2's duration variable.}

\item{d2}{name of equation 2's failure indicator.}

\item{x1}{character vector of covariate names for equation 1's location
index (may be empty for an intercept-only equation).}

\item{x2}{character vector of covariate names for equation 2's location
index.}

\item{nodes1, nodes2}{interior nodes (ascending numeric vectors) for a
piecewise-Gompertz equation 1/equation 2; required exactly when that
equation is \code{"pgompertz"}.}

\item{corr}{logical; if \code{FALSE}, fixes the copula correlation at 0
and fits the two equations independently (see the manual for why this
reduces exactly to two separate univariate fits).}

\item{start}{optional named numeric vector of starting values.}

\item{method}{optimization method passed to \code{maxLik::maxLik}.}
}
\value{
An object of class \code{"lillardhaz"}.
}
\description{
Fits a two-equation simultaneous model in the style of Lillard (1993): a
pair of processes, each either a binary probit outcome or a continuous-
time hazard duration, linked through a single correlation parameter
between their underlying error terms and estimated jointly by maximum
likelihood via a Gaussian copula.
}
\examples{
set.seed(1)
n <- 2000
x1 <- rnorm(n); x2 <- rbinom(n, 1, 0.5)
t1 <- exp(1 - 0.3 * x1 + 0.5 * rnorm(n))
c1 <- -log(runif(n)) / 0.05
t2 <- exp(0.6 + 0.4 * x2 + 0.6 * rnorm(n))
c2 <- -log(runif(n)) / 0.05
d <- data.frame(
  time1 = pmin(t1, c1), event1 = as.integer(t1 <= c1),
  time2 = pmin(t2, c2), event2 = as.integer(t2 <= c2),
  x1 = x1, x2 = x2
)
fit <- fit_lillardhaz("lognormal", "lognormal", d,
                       y1 = "time1", d1 = "event1", y2 = "time2", d2 = "event2",
                       x1 = "x1", x2 = "x2")
summary(fit)
}
